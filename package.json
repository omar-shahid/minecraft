{
  "name": "webcraft",
  "version": "1.1.0",
  "description": "A Minecraft clone in pure JavaScript + WebGL2",
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8000 || npx -y http-server -p 8000",
    "test": "node test/smoke.mjs && node test/textures.mjs && node test/integration.mjs"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
